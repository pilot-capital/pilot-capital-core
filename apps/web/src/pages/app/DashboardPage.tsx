import React from "react";

export default function DashboardPage() {
    return (
        <div>
            <h1>Dashboard</h1>
            <p>
                This is the main app dashboard. Only accessible when logged in.
            </p>
        </div>
    );
}
